version: "3.9"

services:
  app:
    image: dev.chat-app:latest
    container_name: dev.chat-app

    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "3000:3000"

    restart: unless-stopped